# Port the HTTP server listens on
PORT=3000

# Postgres connection string (see compose.yml for local credentials)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/expediente_hrl?schema=public
